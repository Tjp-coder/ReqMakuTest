# 本地开发环境配置
# 通过 mvn -Denv=dev test 使用此文件

# 被测系统地址
baseUrl=http://localhost
port=8080

# 数据库（本机 MySQL）
dbUrl=jdbc:mysql://127.0.0.1:3306/maku_boot?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT
dbUsername=root
dbPassword=root

# 登录账号（敏感信息在 CI 中用环境变量覆盖，不要提交真实密码）
authUsername=admin
authPassword=admin
