# 测试环境配置（远程服务器）
# 通过 mvn -Denv=test test 使用此文件（默认环境）
# 本地开发环境见 config/dev.properties，用 -Denv=dev 切换

# 被测系统地址
baseUrl=http://8.138.30.161
port=8080

# 数据库（与被测服务同一台远程服务器）
dbUrl=jdbc:mysql://8.138.30.161:3306/maku_boot?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT
dbUsername=root
# 真实密码不入库，本地跑测试前用环境变量 DBPASSWORD 覆盖（参考 Env.resolve 的三级回退）
dbPassword=

# 登录账号（敏感信息在 CI 中用环境变量覆盖，不要提交真实密码）
authUsername=admin
authPassword=admin
